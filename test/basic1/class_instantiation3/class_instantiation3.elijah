indexing
	date: "0305210428", "0305210634", "0714210544"

import std.math

class Bar[T] {
	var t: T

	constructor x() {}
}

class Foo {
	var f: SystemInteger
}

class Main {
	main() {
		var bar : Bar[SystemInteger]
		construct bar.x
		bar.t = 13

		var foo = Foo()
		foo.f = 14

		println(foo.f)

		iterate from 1 to 10 with i {
			foo.f = randInt(1, 100)
			print(i)
			print(" ")
			println(foo.f)
		}

		println(bar.t)

	}
}
